// preload.js 对于无UI时候的模板
const PRELOAD_TEMPLATE = `
`;
// HTML 有 UI 的组件模板
const HTML_TEMPLATE = ``;

// plugin.json 的模板
const PLUGIN_JSON_TEMPLATE = ``;



export {
    PRELOAD_TEMPLATE,
    HTML_TEMPLATE,
    PLUGIN_JSON_TEMPLATE
}